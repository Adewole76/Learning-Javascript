const transactionContainer = document.querySelector('.transaction-container');
const transactionsArray = []
const addTransaction =(arr, transcationid, transactiontype, transactionamount, transactiondescription, transactioncategory, transactioncreatedAt) => {
    let transactionObject = {
        transactionid: id,
        transactionType:type, 
        transactio
    }
}